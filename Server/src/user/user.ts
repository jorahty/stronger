export interface User {
  username: string;
  name: string;
  image?: string;
}

export interface UserDetails {
  username: string;
  name: string;
  image?: string;
  location?: string;
  website?: string;
  bio?: string;
}
